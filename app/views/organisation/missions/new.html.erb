<div class="form-page">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-6 col-sm-offset-4 m-auto">
      <div class="form">
      <h1>Crée ta nouvelle mission</h1>
        <%= simple_form_for([:organisation, @mission]) do |f| %>
          <%= f.input :title %>
          <%= f.input :description %>
          <%= f.input :category, as: :select, collection: ["humanitaire 🫶🏻", "environnement 🌱", "animaux 🐶"] %>
          <%= f.input :address %>
          <%= f.input :city %>
          <%= f.input :date, html5: true %>
          <%= f.input :duration %>
          <%= f.input :max_participant %>
          <%= f.input :photos, as: :file, input_html: { multiple: true } %>
          <%= f.submit "J'ajoute cette mission", class: "btn btn-ghost-white mb-3"%>
        <% end %>
      </div>
    </div>
  </div>
</div>
